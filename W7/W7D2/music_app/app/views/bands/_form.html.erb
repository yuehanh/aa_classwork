<%# submit = band.persisted? ? "Update" : "Create" %> <%# rails default to Update Model, Create Model %>

<%= form_with model: band, class: "form" do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <br>
    <%= f.submit submit, class: "button" %>
<% end %>
