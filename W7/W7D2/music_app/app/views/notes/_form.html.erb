<%= form_with(model: note (@band), local: true) do |form| %>

    <%= form.hidden_field :track_id, value: params[:id] %>
  <div class="field">
    <%= form.label :note %>
    <%= form.text_area :note %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
