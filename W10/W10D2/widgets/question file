you'll also want to store that interval's id so you can cancel it in componentWillUnmount, which gets called just before the component is removed. Don't store this in the component's state since it doesn't affect the UI. Instead, just store it directly on this.

why do we need this?